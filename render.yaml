services:
  # Web Service para la aplicación Flask
  - type: web
    name: taskflow-app
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app
    envVars:
      - key: postgresql://taskflow_db_jv9h_user:ig6PV75hd3KQgkMVzm0UBLNCaX4OjxsX@dpg-d8mb1p4m0tmc73cu60lg-a/taskflow_db_jv9h
        fromDatabase:
          name: taskflow-db
          property: connectionString
      - key: PYTHON_VERSION
        value: 3.11.0

databases:
  # Base de Datos PostgreSQL administrada
  - name: taskflow-db
    databaseName: tareas_db
    user: postgres
    plan: free
